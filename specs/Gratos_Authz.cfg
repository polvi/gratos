SPECIFICATION Spec
\* Quiescent states are legal: e.g. all admins revoked and no bootstrap
\* attempt in flight is the (intentional) reopened-bootstrap idle state.
CHECK_DEADLOCK FALSE
CONSTANTS
    Users = {u1, u2, u3}
INVARIANTS
    TypeOK
    BootstrapUnique
    MutatorsWereAdmins
