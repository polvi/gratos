{
  "name": "gratos",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build-demo": "bun --cwd packages/preact build && bun --cwd packages/demo build",
    "build-dash": "bun --cwd packages/preact build && bun --cwd packages/gratos-dash build",
    "dev-dash": "bun --cwd packages/gratos-dash dev",
    "dev-provisioner": "bun --cwd packages/provisioner dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-types": "^1.3.5",
    "wrangler": "^4.58.0",
    "typescript": "^5.9.3"
  }
}
